{
  "name": "error-if-array-item-is-not-number",
  "version": "1.0.2",
  "description": "a function to be called inside a loop that iterates through an array of numbers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "numbers",
    "type-checking",
    "trigger",
    "error"
  ],
  "author": "Steve Thompson",
  "license": "FREE",
  "dependencies": {
    "basic-data-handling": "latest",
    "@writetome51/not": "latest"
  }
}
